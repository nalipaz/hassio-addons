# Configuration file for ddclient generated by debconf
#
# /etc/ddclient.conf

protocol={{protocol}}
use=web, web={{remote_ip_utility_url}}
server='{{server}}'
login='{{login}}'
password='{{password}}'
{{#domains}}{{domain}}
{{/domains}}
